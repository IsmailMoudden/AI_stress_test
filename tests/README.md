# Cross-System Tests

This directory is reserved for tests that cross application boundaries.

- `integration`: service and infrastructure integration tests
- `e2e`: browser or workflow-level tests
- `performance`: load, latency, and capacity tests
- `security`: authorization and data access tests

