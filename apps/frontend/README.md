# Frontend

Placeholder for the future React and TypeScript frontend.

The UI should be dense, precise, and professional. It should prioritize fast analyst workflows, audit visibility, review queues, comparison tables, diff views, and controlled user overrides.

No frontend implementation is included in this bootstrap.

## Demo

`index.html` contains the first static end-to-end mock demonstration. It is dependency-free and uses deterministic mock data only.

Open `apps/frontend/index.html` in a browser to run it.
