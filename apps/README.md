# Applications

This directory contains deployable application surfaces.

- `backend`: future FastAPI service boundary for orchestration, governance, persistence, and integration APIs.
- `frontend`: future React and TypeScript institutional web application.

Business logic should be organized by domain and application service boundaries, not by framework convenience.

