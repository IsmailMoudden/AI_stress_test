# Packages

Shared packages should be introduced only when they remove real duplication or stabilize contracts across applications.

Expected future uses:

- generated API clients
- shared TypeScript types
- shared validation schemas
- common test fixtures

